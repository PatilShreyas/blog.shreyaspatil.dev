---
import type { CollectionEntry } from "astro:content";
import { slugifyStr } from "@/utils/slugify";
import { getPath } from "@/utils/getPath";
import Datetime from "./Datetime.astro";

type Props = {
  variant?: "h2" | "h3";
  postNum?: number;
} & CollectionEntry<"blog">;

const { variant: Heading = "h2", id, data, filePath, postNum } = Astro.props;

const { title, description, ...props } = data;
---

<li class="group my-8 transition-all">
  <a
    href={getPath(id, filePath)}
    class:list={[
      "inline-block text-xl font-bold text-foreground decoration-accent/30 decoration-2 underline-offset-8 transition-all hover:text-accent hover:underline sm:text-2xl",
      "focus-visible:no-underline focus-visible:underline-offset-0",
    ]}
  >
    <Heading
      style={{ viewTransitionName: slugifyStr(title.replaceAll(".", "-")) }}
    >
      {postNum && <span class="text-accent/80 font-mono text-lg mr-2 sm:text-xl">#{postNum}</span>}
      {title}
    </Heading>
  </a>
  <div class="mt-2 opacity-80">
    <Datetime {...props} />
  </div>
  <p class="mt-3 line-clamp-3 text-lg text-foreground/80 leading-[1.6]">{description}</p>
</li>
